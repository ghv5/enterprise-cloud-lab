platform-infra installed.

Next checks:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get svc -n {{ .Release.Namespace }}

Expected services:
  {{ include "platform-infra.mysql.fullname" . }}
  {{ include "platform-infra.redis.fullname" . }}
  {{ include "platform-infra.kafka.fullname" . }}
  {{ include "platform-infra.nacos.fullname" . }}
