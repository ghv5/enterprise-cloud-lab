# observability-addons

Helm chart for the local RuoYi observability lab.

Current scope:

- `ServiceMonitor` for APISIX metrics
- `ServiceMonitor` for Nacos metrics
- Basic Auth Secret used by the Nacos metrics endpoint
